# Core ML
torch>=2.0.0
transformers>=4.36.0
datasets>=2.16.0

# TransformerLens for interpretability
transformer-lens>=1.14.0

# Utilities
einops>=0.7.0
numpy>=1.24.0
tqdm>=4.66.0

# Logging (required by transformer-lens)
wandb>=0.15.0

# Feature labeling
python-dotenv>=1.0.0
anthropic>=0.30.0

# UI / Visualization
gradio>=4.0.0
plotly>=5.18.0

# New web server (replaces Gradio for custom HTML/JS/CSS UI)
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9